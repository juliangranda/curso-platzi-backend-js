pwd: ver ubicacion o url para utilizarla desde el navegador

console.group() - nos permite agrupar los console.log() en grupo para ser mas ordenados al momento de mostrarlos
console.groupend() - nos permite cerrar la agrupacion de los console.log() para evitar mezclarlos con otros console.group() este es obligario siempre cerrarlo

imprimir en consola las variables como si fuera un objeto
console.log({
    precioOriginal,
    descuento,
    porcentajePrecioConDescuento,
    precioConDescuento
});



    //metodos para recorrer arrays
    //reducer: se utiliza para por medio de un argumento de una funcion(callback) este puede recorrer todos los
    //elementos del array e irlos sumando uno por uno para asi reducir el array

    const sumaLista = lista.reduce(
        function (valorAcumulado = 0, nuevoElemento) {
          return valorAcumulado + nuevoElemento;
        }
      );

//parseInt: convierte un numero float a int

metodo de array : .sort() =  se usa para organizar listas de array de forma ascendente
ex: lista1.sort((a,b) => a - b);


//Object.entries() : acepta un argumento o un objeto que queramos convertir a un array
const lista1Array = Object.entries(lista1Count);


//herramienta de array map: map itera sus elementos en orden de insercion un bucle for que devuelve una matriz de [key,value] para cada iteracion.
lista.map(function(){});